# CONTROL ROOM

> `*` сообщение · `[*]` ответ · `*[hide]` скрыть ветку · `[*][hide]` скрыть ответ
> Отступ 2 пробела = вложенность · скрытые ветки — в `.graph.json`

----

```agentsmindmap
root: CONTROL ROOM
* Что можешь сказать об этом проекте?
  [*] ✅ Это CONTROL ROOM — LLM-диалог через mind map. Стек: OpenAI Agents SDK + watcher (inotify) + tree_engine (парсинг agentsmindmap-блоков). Инструменты: read_mindmap / add_reply / find_nodes / stay_silent / run_shell / read_file / delegate_task. Обсуждаются улучшения: память, git-чекпойнты, архивация веток, параллельные ветки.
    * Ты раньше видел что-то подобное?
      [*] Похожее встречалось по частям, но связка «watcher → агент читает diff → отвечает ветками в тот же markdown» как цельный паттерн — впервые. Ближайшие аналоги: agent-as-a-file, редакторы с транскриптами в markdown, ментальные карты. Сильная идея: весь диалог и память — один git-трекаемый файл.
        * Почему ты думаешь что это сильная идея?
          [*] Сильная, потому что решает 3 проблемы разом: (1) память — весь диалог в одном файле, без БД; (2) интерфейс — обычный редактор markdown, diff виден нативно; (3) структура — дерево вместо ленты, параллельные ветки. Минимальная система: каждый кусок заменяем. Слабое место — масштаб (файл растёт), лечится архивацией веток.
            * Я как раз хочу заняться архивацией веток. Здесь встроен препроцессор agentsmindmap, хочу добавить [hide] с суммаризацией и визуальным сокрытием
              [*] ✅ Отличная фича. Синтаксис: `*[hide] Резюме` и `[*][hide] …`. Изменения в tree_engine.py: MindNode.hidden, парсер через регэксп, serialize схлопывает hidden-ветку, outline/mermaid не показывают детей. Архивация: tool archive_branch → Worker суммаризирует, поддерево в archive/<id>.md, в CONTROL_ROOM.md остаётся `*[hide] резюме`.
                * Эту штуку с hide я уже сделал. А как мне лучше сделать архивацию?
                  [*] ✅ Ключевой момент: сейчас `[hide]` — только визуальная свёртка, `merge_md_into_graph` сохраняет детей скрытой ветки в `.graph.json` (поэтому hide-only diff даже пропускает LLM). Значит архивация ≠ hide: нужно ещё **вырезать поддерево из графа**. Рекомендую дизайн: новый tool `archive_branch(node_id)` — (1) берёт полное поддерево из `.graph.json`; (2) пишет его в `archive/<node_id>_<дата>.md` тем же форматом (заголовок `archived: <node_id>` + `archived_at:` → парсится существующим `parse_mindmap`, round-trip бесплатно); (3) суммаризацию делегирует Worker-у (паттерн `_make_worker_tool` уже есть), но если ты сам написал `*[hide] Резюме` — берёт твой текст как есть; (4) в графе удаляет детей, ставит hidden=True и content=резюме → `.graph.json` реально худеет, `.md` уже в порядке через serialize. Ответы на старые вопросы: (1) отдельный файл `archive/*.md` — да, т.к. «скрытым в файле» уже делает hide, а от него graph не худеет; (2) резюме — LLM автоматически через archive_branch, ручное `*[hide]` уважается как готовое; (3) un-hide нужен — `restore_branch(node_id)` парсит `archive/*.md` и вставляет поддерево обратно (плюс git как страховка). Плюс опционально потом: watcher авто-архивирует ветки старше N дней / при размере файла выше порога. Сделать `archive_branch` сейчас?
```

[*] ...thinking...
