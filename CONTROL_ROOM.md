# CONTROL ROOM

> `*` сообщение · `[*]` ответ · `*[hide]` скрыть · `*[archive]` архивировать · `*[archive: причина]`
> Отступ 2 пробела = вложенность · скрытые/архивные ветки — в `.graph.json` / `archive/`

----

```agentsmindmap
root: CONTROL ROOM
* Предложи улучшения по этому проекту, минимум  5
* Предложи улучшения по этому проекту, минимум  5
* Предложи че нить
* Предложи улучшения по этому проекту, минимум  5
  [*] ✅ Изучил код (agent.py, watcher.py, tree_engine.py, archiver.py) и проверил гипотезы тестами. Улучшения, по приоритету:
    1. 🐛 **Архивирование теряет данные (критично)** — при `*[archive]` в .md `merge_md_into_graph` удаляет детей узла из .graph.json ДО того, как watcher вызовет `archive_branch` (условие `node.archived and node.children` уже false). Поддерево не попадает в archive/ и бесследно исчезает. Проверил скриптом: после merge дети пропали, архивируемых узлов — 0. Фикс: сохранять детей для archived-узлов при merge или архивировать до merge.
    2. 🐛 **`restore_branch` сбрасывает флаг не у того узла** — `tree.root.children[-1]` вместо узла, возвращённого `attach_subtree`. Восстановление вложенной ветки (не последнего корневого ребёнка) чистит архивный флаг/контент у постороннего узла.
    3. 🔓 **Проверка пути в `read_file` обходится** — `str(target).startswith(str(root))` пропускает соседние каталоги с префиксным совпадением (`inline-vibe2/...` → True, проверил). Фикс: `Path.is_relative_to()` или `os.path.commonpath`.
    5. 🛡 **Нет обработки ошибок вокруг `Runner.run_sync`** — исключение роняет watcher в `--once`, оставляет грязный .md и `last_content`; в watch-режиме возможен цикл повторных срабатываний. Нужен try/except + логирование + обновление состояния.
    6. 📦 **pyproject.toml неполный** — в зависимостях только `watchfiles`, но код импортирует `agents` (openai-agents) и `openai` (archiver). `pip install .` падает. Добавить `openai-agents>=0.18` и `openai`.
    7. 🏷 **Нестабильные позиционные ID** — удаление/вставка узла в середине сдвигает ID (root.3→root.2), merge путает узлы и может потерять скрытые поддеревья. Фикс: стабильные UUID в .graph.json, а в .md — только позиции.
    8. 🧹 **Мёртвый код/расхождения**: `compute_diff` используется только тестами (watcher берёт `diff_trees`); `--interval` не используется вовсе; README (94 теста) vs AGENTS.md (85) не сходятся; нет `test_archiver.py` — архиватор не покрыт тестами (поэтому баг №1 и прошёл).
    9. ⚙️ **`.env` парсится вручную** (`partition("=")`) — нет кавычек, `export`, пробелов; стоит взять python-dotenv. Также нет CI/pre-commit.
    10. 🧠 **`run_shell` без песочницы** — prompt-injection из .md может заставить агента выполнить произвольные команды. Стоит allowlist или явное подтверждение.
* Предложи че нить
```



[*] ...thinking...
