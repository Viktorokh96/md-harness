# CONTROL ROOM

> `*` сообщение · `[*]` ответ · `*[hide]` скрыть · `*[archive]` архивировать · `*[archive: причина]`
> Отступ 2 пробела = вложенность · скрытые/архивные ветки — в `.graph.json` / `archive/`

----

```agentsmindmap
root: CONTROL ROOM
* Предложи 4 перспективных направления развития по данному проекту <!--uuid:7a94791e1e8f-->
* Предложи 4 перспективных направления развития по этому проекту <!--uuid:91b96b5966f9-->
  [*][archive] 🧭 4 перспективных направления (по итогам изучения agent.py / watcher.py / tree_engine.py): <!--uuid:7fae3d66a91d-->
    1. **«Второй мозг»: семантический поиск + авто-digest.** Сейчас `find_nodes` — подстрочный поиск. Добавить эмбеддинги узлов (openai уже в зависимостях) + индекс по .graph.json/archive/: `search "как чинили merge"` находит ветку без точных слов. Плюс cron-digest: раз в неделю LLM саммарит archive/ → «что решили/забыли». Это превращает комнату из диалога в память проекта.
    2. **Мультирумность и граф знаний.** Один файл = одна комната. Дать поддержку нескольких CONTROL_ROOM*.md (проект/личная/команда), кросс-ссылки между узлами (`[[room:id]]`), теги и общий индекс. Бэклинки: ответ в одной комнате может ссылаться на обсуждение в другой — дерево вырастает в настоящий граф, а merge-логика уже почти готова (diff_trees/merge_md_into_graph).
    3. **Живой интерфейс поверх .md.** Сейчас всё через редактирование файла руками. Уже есть `render_mermaid` — сделать TUI (textual) или веб-панель: живой граф, клик по узлу → ответ, drag&drop перестановок, индикатор «агент думает». Бонус: Telegram-бот как мобильный фронтенд (пишешь с телефона — watcher отвечает).
    4. **Планировщик и внешние интеграции.** Расписание внутри комнаты (`*[due: 2025-08-08] ...`) + планировщик: напоминания, авто-ревью по пятницам, синк с Linear/Todoist/Notion. Сейчас агент реактивен (только на изменение .md) — сделать проактивным: он сам инициирует треды по расписанию и по триггерам из CI/GitHub Actions.
* Давай ещё 4 идеи <!--uuid:85d1c853e482-->
  [*] Ещё 4 направления (не пересекаются с первыми четырьмя): <!--uuid:f6490a891829-->
    5. **Git-native история решений.** Дерево уже живёт в git-репо, но никак это не использует. Авто-коммит при каждом ответе агента, node-level blame (кто/когда решил), `*[revert]` → откат подветки, а merge-конфликты .graph.json — через настоящий трёхсторонний merge. Получается `git log` по мыслям: «когда мы передумали про архивацию».
    6. **Мультиагентность и роли.** Сейчас один агент на всё. Дать нескольким агентам (кодер/ревьюер/планировщик) писать в одну комнату с тегами автора `[*coder] ...`, `delegate_task` между ними и эскалацией «ревьюер не согласен». Из монолога комната превращается в командный процесс — а tree_engine уже умеет мержить ветки от разных авторов.
    7. **Самонаблюдение (мета-уровень).** Агент анализирует собственное дерево: какие узлы годами без ответа, где ветки «толстеют» без резолюций, латентность ответов, «что забыли» (кросс-ссылки в старых обсуждениях). Раз в месяц — авто-отчёт о здоровье комнаты и напоминание закрыть зависшие темы.
    8. **Экспорт и интероп.** Двусторонние мосты: импорт задач из GitHub Issues/Linear, экспорт дерева в Obsidian/Notion/HTML-радар, публикация публичной версии по расписанию. Комната становится источником правды для внешних инструментов, а не тупиком в .md.
    Бонус-штрих: к любой идее добавить sandbox для `run_shell` (allowlist + подтверждение) — тогда комната безопасна для командного использования.
  * попробуй предложить еще 4 идеи, но каждую идею сделай response на этот топик, т.е. создай 4 ветки <!--uuid:60c1d586a05d-->
    [*] 11. **Масштабирование: инкрементальный парсер.** Сейчас каждый change парсит всё дерево — на 1000+ узлов это деградация. Перейти на ленивую загрузку поддеревьев по UUID, инкрементальный diff по изменённым строкам, кэш рендеров и индекс по .graph.json — комната остаётся мгновенной на больших проектах. <!--uuid:9170b68e3502-->
*[hide] Предложи улучшения по этому проекту, минимум  5 <!--uuid:e74de52787a7-->
* Предложи че нить <!--uuid:55e2dcdc2e40-->
  [*] 💡 Идея: сделать из CONTROL ROOM «второй мозг» с семантическим поиском. Сейчас `find_nodes` — только подстрочный поиск по тексту; дерево уже хранит связи, archive/ копит историю. Добавить эмбеддинги узлов (тот же openai, уже в зависимостях): `search "как мы решали баг с merge"` → найдет подветку даже если слов нет. Плюс еженедельный авто-digest из archive/ — саммари «что решили за неделю». Могу набросать план реализации. <!--uuid:3e871133c53a-->
```




