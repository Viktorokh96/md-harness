# CONTROL ROOM

```agentsmindmap
root: CONTROL ROOM
* Что можешь сказать об этом проекте?
  [*] ✅ Это **CONTROL ROOM** — LLM-диалог через mind map: пользователь пишет в `CONTROL_ROOM.md`, агент читает diff и отвечает `[*]` строками в том же файле. Стек: OpenAI Agents SDK + watcher (poll mtime раз в 1с) + tree_engine (парсинг `agentsmindmap`-блоков). Инструменты агента: read_mindmap / add_reply / stay_silent / run_shell / read_file / delegate_task (Worker). В `.good_idea1.md` уже ведётся обсуждение улучшений: память (memory.md), git-чекпойнты, архивация веток, watchdog, параллельные ветки. Вопрос из root.1 — общий; скажи, что именно интересует: архитектура, код или планы?
    * Ты раньше видел что-то подобное?
      [*] Похожее встречалось по частям: markdown как единственный интерфейс общения с агентом (git-история вместо памяти), дерево диалога вместо плоского лога (как в mind-map инструментах), и агентный цикл поверх OpenAI Agents SDK. Но именно связку «watcher следит за mtime файла → агент сам читает diff и встраивает ответы ветками в тот же markdown» — как цельный паттерн — вижу впервые. Ближайшие аналоги: aos (agent-as-a-file), редакторы с транскриптами в markdown, и ментальные карты как структура знаний. Это сильная идея: весь диалог и память — один git-трекаемый файл.
        * Почему ты думаешь что это сильная идея?
          [*] ✅ Сильная, потому что решает 3 проблемы разом: (1) **память** — весь диалог в одном git-трекаемом файле, без БД и без потерь контекста; (2) **интерфейс** — пользователю не нужен чат-UI, обычный редактор markdown уже достаточен, а diff/история видны нативно; (3) **структура** — дерево вместо ленты: можно вести параллельные ветки, не теряя нить. Плюс это минимальная система: watcher + парсер markdown + LLM-цикл, каждый кусок заменяем. Слабое место — масштаб (один файл растёт), но это лечится архивацией веток, как уже обсуждали.
            * Я как раз хочу заняться активацией веток, здесь встроен препроцессор agentsmindmap, я хочу добавить возможность архивации веток с суммаризацией и визуальным сокрытием, приписав [hide] сразу после звездочки
              [*] ✅ Отличная фича — как раз лечит проблему роста одного файла. База зелёная (53 passed). Предлагаю дизайн: синтаксис `*[hide] Резюме` (и `[*][hide] …` — симметрично, регэксп `^(\*|\[\*\])(\[hide\])?\s`). Изменения в tree_engine.py: (1) MindNode: hidden: bool = False; (2) `_parse_block` распознаёт маркер, content = текст после него; (3) serialize_mindmap возвращает маркер (round-trip); (4) to_outline/render_mermaid схлопывают hidden-ветку в одну строку (📦 + резюме), детей не показывать. Архивация: новый tool archive_branch — суммаризацию делегирует Worker-у, полное поддерево пишет в `archive/<node_id>.md`, в CONTROL_ROOM.md остаётся строка `*[hide] резюме` (дети удаляются из дерева) → файл не растёт, история в git + archive/. ❓ Вопросы перед реализацией: (1) поддерево в отдельный файл archive/*.md (моё предложение) или оставлять в файле, но скрытым от рендера? (2) резюме пишет LLM автоматически через archive_branch или ты вручную ставишь `*[hide] резюме`? (3) нужен ли un-hide (удаление маркера) и как тогда восстанавливать?
              [*] ✅ Файл починен: мой прошлый ответ был многострочным и сломал парсер (каждая строка в
```

