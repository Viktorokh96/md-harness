# CONTROL ROOM

> `*` сообщение · `[*]` ответ · `*[hide]` скрыть · `*[archive]` архивировать · `*[archive: причина]`
> Отступ 2 пробела = вложенность · скрытые/архивные ветки — в `.graph.json` / `archive/`

----

```agentsmindmap
root: CONTROL ROOM
* Сколько ещё идей можешь дать по типу той что уже реализована в этом проекте? хотя 3 бы дай <!--uuid:7ad9575625a6-->
  [*] ✅ Идей такого же типа (маркер → препроцессор → LLM, как [hide]/[archive]) могу дать хоть десяток. Вот 4 сильнейших: <!--uuid:8e7d9616faa1-->
  [*] ▶ `*[ ]` / `*[x]` — задачи в дереве: чекбоксы прямо в маркере, препроцессор рендерит прогресс в outline (▸ 3/7), агент по запросу выдаёт сводку «что осталось» и следит за незакрытыми. Тот же паттерн, что [hide] — чисто синтаксис, без нового кода хранения. <!--uuid:acbac28f38cf-->
  [*] ▶ `*[due: 2026-08-08]` — дедлайны и напоминания: препроцессор подсвечивает просроченные узлы, агент при следующем ходе сам напоминает о приближающихся сроках. Проактивность без планировщика — просто маркер + проверка при чтении diff. <!--uuid:80e92756a627-->
    * Актуализируй эту идею в ideas каталоге, расширь, дополни <!--uuid:ade7386d6980-->
      [*] ✅ Идея актуализирована и расширена в `ideas/due.md` (было 4 строки → стало 76). Что добавлено: (1) полный синтаксис — дата, время, относительные сроки `+3d`, комбинации с `[x]`/`[hide]`/`[archive]`; (2) таблица статусов с иконками (overdue 🔴 / due_today 🟠 / soon 🟡 / open 📅 / done ✅), вычисляемых препроцессором без LLM; (3) план реализации по модулям: `tree_engine.py` (парсер маркера → `due_date`, `overdue_days`, сортировка), `agent.py` (новый инструмент `due_report()` + хук-напоминание + антиспам через `last_reminded`), `watcher.py` (проверка при diff, без cron); (4) 5 UX-сценариев, edge cases, список тестов и метрики. Открытые вопросы вынесены в конец файла (сортировка по умолчанию? частота напоминаний? `due-alert: off`?). <!--uuid:581fabb42100-->
  [*] ▶ `[[root.2.1]]` / `*[ref]` — кросс-ссылки и бэклинки между ветками: препроцессор строит индекс ссылок, внизу узла рендерится «связанные обсуждения». Дерево вырастает в граф знаний, merge-логика уже почти готова в tree_engine. <!--uuid:2f13c2dab9a0-->
    * Как дела? <!--uuid:3757006f3ddb-->
  [*] ▶ `*[digest]` — авто-сводка ветки без выноса: LLM одним абзацем резюмирует поддерево прямо в узел (как [archive], но ветка остаётся на месте и редактируемой). Отлично для «что решили в root.3» перед новой встречей. <!--uuid:95d8d23758a2-->
* запусти bash команду date и дай её вывод <!--uuid:d106ea72a936-->
  [*] ✅ `date` → `Сб 01 авг 2026 03:14:00 MSK` <!--uuid:997f4cb1a683-->
    * а какая сейчас версия python? <!--uuid:a3613c4e757f-->
```
