# CONTROL ROOM

> Mind map dialogue — каждый tab = подветка обсуждения.
> `*` = реплика пользователя, `[*]` = реплика агента.

* Привет
	[*] ✅ Привет! Мне нужно понять как ещё можно улучшить текущий CONTROL ROOM механику. Есть идеи?
		[*] ✅ Изучил код — вот идеи по улучшению механики:
			[*] ▶ Параллельные ветки: сейчас агент обрабатывает весь файл одним прогоном — можно запускать отдельного агента на каждую ветку
			[*] ▶ Watchdog вместо polling (mtime раз в 1с) — мгновенный отклик через inotify/file-watcher
				* Почему сразу так не сделали?
			[*] ▶ Авто-архив длинных веток: свёртка старых обсуждений в саммари-ноду, чтобы контекст не разрастался
				* Это мне тоже в голову пришло, это же идеально! А как лучше помечать такие ветки? Вот допустим я хочу архивировать ветку, каким жестом мне это сообщить? я не хочу случайно её архивировать
			[*] ▶ Память агента (memory.md) + git-чекпойнты CONTROL_ROOM.md — история и undo
				* Да это очень важная вещь, как будем её делать?
			[*] ❓ Что внедрить первым?
					[*] ✅ Polling выбран за простоту и кроссплатформенность: mtime-чек (раз в 1с) работает везде без зависимостей, а inotify привязан к Linux
						[*] ▶ File-watcher можно добавить позже как оптимизацию — механика файла при этом не меняется
					[*] ✅ Предлагаю явный маркер: напиши `[🗄 архив]` в конце ветки — агент свернёт её только на следующем проходе
						[*] ✅ Плюс подтверждение: перед свёрткой агент покажет саммари и спросит «архивирую?» — двойная защита от случайного архива
					[*] ▶ memory.md: агент дописывает факты с датой после каждого диалога (ключевые решения, предпочтения)
						[*] ▶ git: после каждого хода агент делает commit CONTROL_ROOM.md + memory.md → undo через git revert/checkout
					[*] ✅ Порядок внедрения: 1) память + git-чекпойнты (низкий риск, сразу история/undo) → 2) маркер архивации → 3) watchdog → 4) параллельные ветки
						[*] ❓ Начинаем с memory.md + git-чекпойнтов?
